# Notion auto login

The project uses [`pnpm`](https://pnpm.io/) as package manager.

Install dependencies:

```
pnpm install
```

Build the project:

```
pnpm build
```

Then run:

```
pnpm start
```
